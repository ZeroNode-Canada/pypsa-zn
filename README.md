```text
SPDX-License-Identifier: Apache-2.0
Copyright 2025 ZeroNode
Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at
http://www.apache.org/licenses/LICENSE-2.0
Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
README.md
Results under /devnet-stress-vectors are generated outputs and not part of the licensed source code.
------------------------------------------------------------------------------
```

# DevNet – Datacenter BYOG DoE (PyPSA)

This repository contains a **research-grade DevNet workflow** for studying datacenter
**Bring-Your-Own-Generation (BYOG)** impacts on grid feasibility, congestion, and
Locational Marginal Prices (LMPs), using **PyPSA**.

The project is designed as a **deterministic, lightweight “USA-lite” 6-bus system**
that supports systematic stress testing, reproducible plots, and supervisory reporting,
while remaining structurally compatible with PyPSA-USA scale-up.

---

## High-level workflow

The intended execution flow is **menu-driven** via `devnet_menu.py`:

```bash
    1. Generate CSV templates → devnet_cfg.py
    2. Edit CSV inputs        → ./devnet_config/*.csv
    3. Build baseline SLD     → devnet_sld.py
    4. Build DC BYOG SLD      → devnetDC_sld.py
    5. Run DoE sanity         → devnet_doe.py
    6. Stress / asymptotes    → devnet_stress.py
    7. Demo scenarios         → demo/pypsa_zn_demo.py
    8. Generate plots         → devnet_*_plot.py
```


**Important:**
* Run devnet_cfg.py first to generate user-editable CSV templates.
* Edit ./devnet_config/*.csv before building the SLDs.
* devnet_sld.py and devnetDC_sld.py build PyPSA networks from CSV inputs.
* devnet_stress.py is the primary research loop.
* Reference outputs for replication are under ./devnet-stress-vectors/.

---

## Conference Demonstration Framework

A presentation-oriented demonstration framework is provided under:

```text
./demo/
```
This subsystem builds on the same DevNet modeling engine while providing:
* Interactive scenario selection
* Story plot generation
* Presentation dashboards
* Conference kiosk support

Refer:  
./demo/README.md

for demonstration-specific workflows and operator guidance.

---

## Entry point

### CSV configuration workflow

DevNet is now parameterized through user-editable CSV files generated by:

```bash
python devnet_cfg.py
```
This creates: 

```bash
./devnet_config/
  ├─ devnet_buses.csv
  ├─ devnet_lines.csv
  ├─ devnet_assets.csv
  ├─ devnet_dc.csv
  └─ devnet_carriers.csv
```

These files define:
* DevNet 6-bus node names and coordinates
* transmission topology and line capacities
* per-bus generation capacity, load, and marginal cost
* datacenter bus association
* datacenter load, BYOG capacity, and BYOG marginal cost
* PyPSA carrier definitions

The 6-node DevNet structure is intentionally fixed for interpretability and regression testing.

### Run the menu
```bash
    python devnet_menu.py
```

This launches an interactive CLI that:

* Runs scripts in the correct order
* Preserves the active Python environment
* Keeps console output structured and readable

---

## Script overview

### `devnet_sld.py` — Baseline network construction

* Reads user-configurable CSV inputs from `./devnet_config/`
* Builds the **6-bus USA-lite DevNet baseline**
* Exports PyPSA CSVs, logs, and SLD image
* Supports suffixed build directories such as `devnet-sld1`, `devnet-sld2`, ...
* Output directory:

```bash
    ./devnet-sld/
        ├─ *.csv
        ├─ plots/
        └─ logs/
```

### `devnetDC_sld.py` — Datacenter BYOG network construction

* Reads the same baseline CSV inputs from `./devnet_config/`
* Adds datacenter load + BYOG metadata from `devnet_dc.csv`
* Exports a PyPSA-ready `devnetDC-sld` network
* Supports suffixed build directories such as `devnetDC-sld1`, `devnetDC-sld2`, ...

### `devnet_doe.py` — Sanity & baseline validation

* Loads exported DevNet CSVs
* Confirms DC-OPF feasibility and baseline behavior
* Acts as a “known-good” checkpoint
* Output directory:

```bash
    ./devnetDC-sld/
        ├─ *.csv
        ├─ plots/
        └─ logs/
```

### `devnet_stress.py` — Core research engine

* Interactive stress testing and asymptote discovery
* Supports:

  * Load scaling (`k_load`)
  * Corridor derating (`k_line`)
  * Generator marginal cost perturbations (`mc_bus`)
  * Datacenter BYOG ECON MODE
    * `byog_mc`
    * `dc_p_set`
    * `dc_p_nom`

* Each committed run generates:

```bash
  ./devnet-sld/stress_out/
    ├─ c1_*.csv
    ├─ c1_dashboard.md
    ├─ c2_*.csv
    ├─ c2_dashboard.md
    └─ index.html
```

* `index.html` is automatically regenerated after each commit.
* Core simulation logic is implemented in:
* To ensure modelling consistency, both devnet_stress.py & demo/pypsa_zn_demo.py; reuse the same modeling engine through:

```text
lib/devnet_stress_lib.py
```


For replication of published results, see **Replication notes (reference runs)** below,
including `devnet_stress_tc.md` and `DevNet Stress Report_5.html`.
Refer: [Replication notes (reference runs)](#replication-notes-reference-runs)

### DevNet Result Plots

The devnet_*_plot.py scripts below, expect a consolidated devnet_plots.xlsx @./devnet-stress-vectors/  
The plots generated are output @./devnet-stress-vectors/  
Refer: [Replication notes (reference runs)](#replication-notes-reference-runs)

### `devnet_load_plot.py` — Load vs system metrics

* Produces a 4-panel stacked plot:

  1. System objective (cost)
  2. LMP spread
  3. Maximum line loading (p.u.)
  4. Near-binding constraint count
* Used to identify:

  * Feasibility boundaries
  * Linear cost scaling with load
* Output:

```bash
    ./devnet-stress-vectors/load_vs_metrics.png
```

This plot is generated from the consolidated workbook `devnet_plots.xlsx`
assembled as described in **Replication notes (reference runs)**.
Refer: [Replication notes (reference runs)](#replication-notes-reference-runs)

### `devnet_lmp_plot.py` — MC table + LMP heatmap

* Composite visualization:

  * Left: MC perturbation table (per bus)
  * Middle: LMP spread heatmap (case-aligned)
  * Right: metrics panel:

    * max_loading_pu
    * near_bind_ct
    * objective sparkline
    * top congested lines
* Directly links:
  **perturbation → congestion → LMP separation**
* Output:

```bash
  ./devnet-stress-vectors/heatmap_lmp_spread.png
```

This plot consumes LMP spread test cases extracted from the reference stress report
and compiled into `devnet_plots.xlsx` (see **Replication notes (reference runs)**).
Refer: [Replication notes (reference runs)](#replication-notes-reference-runs)

### `devnet_line_plot.py` — Line deration vs system metrics

* Produces a 4-panel stacked plot against **transmission line deration (k_line)**:

  1. System objective (cost)
  2. LMP spread
  3. Maximum line loading (p.u.)
  4. Near-binding constraint count

* X-axis is ordered as:
  **1.0 → 0.8 → 0.6 → 0.4 → 0.2 → 0.1**

* Used to isolate **pure network capacity stress** effects independent of load growth.

* Output:

```bash
  ./devnet-stress-vectors/line_vs_metrics.png
```

Line-deration results are sourced from manually extracted reference cases and compiled
into `devnet_plots.xlsx` as documented in **Replication notes (reference runs)**.
Refer: [Replication notes (reference runs)](#replication-notes-reference-runs)

---

## Visualization & reporting

After running stress cases:

* Open the interactive report:

```bash
./<selected-devnet>/stress_out/index.html
```
* Regenerate plots using menu options for:
  * load stress
  * marginal cost / LMP spread heatmap
  * line deration

**Note:** Plot scripts consume reference workbook inputs under `./devnet-stress-vectors/`.

- Primary input:
  - `devnet_plots.xlsx` (consolidated workbook)

- Source:
  - Derived from reference artifacts:
    - `devnet_stress_tc.md`
    - `DevNet Stress Report_5.html`

- Workflow:
  1. Run `devnet_stress.py` to generate stress outputs (optional, for new studies)
  2. Use provided reference workbook in `./devnet-stress-vectors/` for deterministic plotting
  3. Alternatively, manually extract and update `devnet_plots.xlsx` from new stress runs

> Plot scripts do **not** directly read `stress_out/` outputs.  
> They operate on the curated workbook `devnet_plots.xlsx` for reproducibility and publication consistency.

---

## Replication notes (reference runs)

This repository includes **reference artifacts** to support replication of published
DevNet stress results without requiring an identical interactive replay.

### Reference stress cases and reports

- **Test case definitions**
  - See: `devnet-stress-vectors/devnet_stress_tc.md`
  - Documents the exact set of stress scenarios executed (load, LMP, and line-deration cases).

- **Reference stress report**
  - See: `devnet-stress-vectors/DevNet Stress Report_5.html`
  - Snapshot of `index.html` corresponding to a full execution of
    `devnet_stress_tc.md`.

### Manual extraction workflow (used for plotting)

The composite plotting scripts (`devnet_*_plot.py`) consume a consolidated workbook
`devnet_plots.xlsx`, which is constructed as follows:

1. From **DevNet Stress Report_5.html**, manually extract:
   - Load stress cases → `devnet_load_tc.xlsx`
   - LMP spread cases → `devnet_lmp_tc.xlsx`
   - Line deration cases → `devnet_line_tc.xlsx`

2. Place the extracted files in: `devnnet-stress-vectors/`

3. Manually compile the extracted tables into a single workbook: `devnet_plots.xlsx` with each table placed under the expected sheet names. 

4. **IMPORTANT Note:**  
Ensure devnet_plots.xlsx Header Row starts at Excel Sheet ROW #5.  
HDR_ROW var defined @ devnet_*_plot.py scripts:  

  "...# ----- Read Excel with explicit header row -----  
   HDR_ROW = 4  # zero-based index (row 6 in Excel)..."

5. A **reference copy** of `devnet_plots.xlsx` corresponding to
**DevNet Stress Report_5.html** is provided in: `devnet-stress-vectors/`

This approach ensures deterministic reproduction of plots while keeping the
interactive `devnet_stress.py` workflow flexible and research-oriented.

### Reference runs

This repository includes selected reference DevNet runs for sanity checking and replication.

```text
devnet-reference-runs/
  ├─ devnet-sld-20Mar2026/
  └─ devnetDC-sld-15Apr2026/
```

These folders are **reference snapshots** from earlier validated runs:

* `devnet-sld-20Mar2026/`
  Baseline 6-bus DevNet without datacenter BYOG

* `devnetDC-sld-15Apr2026/`
  6-bus DevNet with datacenter BYOG metadata

They are provided for:

* sanity comparison
* historical traceability
* replication support
* reviewer confidence

Active runs should be generated using:

```bash
python devnet_menu.py
```

Recommended sequence:

```bash
devnet_cfg.py
devnet_sld.py
devnetDC_sld.py
devnet_stress.py
devnet_*_plot.py
```

Generated working folders such as `devnet-sld/`, `devnetDC-sld/`, and suffixed test folders are intentionally ignored by `.gitignore`.


---

## Design philosophy

* **Research-grade determinism** (no hidden state)
* **Fast iteration** (small network, structured sweeps)
* **Clear causal traceability**

  * perturbation → constraint → LMP
* **Supervisor-ready artifacts**

  * CSVs, dashboards, publication-quality plots

---

## Dependencies

* Python 3.10+
* PyPSA
* pandas
* numpy
* matplotlib
* openpyxl

(Use the same environment for all scripts; `devnet_menu.py` preserves it.)

---

## Release diligence

Before publishing or pushing a release candidate:

- Confirm license header is present in source files.
- Confirm `LICENSE` is included at repo root.
- Confirm `NOTICE` is included if required.
- Confirm no private logs, credentials, local paths, or OneDrive-specific artifacts are committed.
- Confirm generated cache/build artifacts are excluded through `.gitignore`.
- Confirm reference artifacts under `devnet-stress-vectors/` are intentional and reproducible.
- Confirm README workflow matches `devnet_menu.py`.
- Confirm `devnet_cfg.py → devnet_sld.py → devnetDC_sld.py → devnet_doe.py → devnet_stress.py` runs cleanly.


---

## Status

* Stable
* Reproducible
* Actively used for PhD research on datacenter BYOG and grid stress behavior

---

## Next steps (planned)

* Release tagging and repo cleanup
* Automated regression test harness
* Analysis module for post-processing and hypothesis tests
* Scaling bridge to PyPSA-USA
* Optional web-facing simulation interface for ZeroNode.ca

---

## Environment & setup

The `pypsa-zn` workflow has been **developed and tested** in the following environment:

### Tested environment

- **Operating system**
  - Windows 11 Pro (x64)  
    - Primary development and test environment.
  - Ubuntu 22.04 LTS (x86_64)  
    - Preliminary validation only.  
    - Full replication runs pending.  
    - Replication steps may differ from Windows due to native Linux server graphics
      support limitations (headless environments).

- **Python**
  - Python **3.10.x**
  - Python **3.11.x** (verified working)

- **Core packages**
  - pypsa
  - numpy
  - pandas
  - matplotlib
  - openpyxl
  - scipy
  - networkx

- **Solver**
  - HiGHS (via PyPSA default configuration)
  - Tested using PyPSA’s built-in solver interface

### Recommended setup (clean install)

```bash
python -m venv pypsa-zn-env
source pypsa-zn-env/bin/activate   # Linux / macOS
pypsa-zn-env\Scripts\activate      # Windows

pip install --upgrade pip
pip install pypsa numpy pandas matplotlib scipy networkx openpyxl
```

---

## Academic contribution

This work contributes a **research-grade, reproducible framework** for studying datacenter
Bring-Your-Own-Generation (BYOG) impacts on power system feasibility, congestion, and
locational marginal prices (LMPs). By combining a deliberately lightweight but structurally
faithful 6-bus “USA-lite” DevNet with systematic stress testing (load growth, corridor
derating, and marginal cost perturbations), the framework enables clear causal tracing from
datacenter-driven perturbations to binding constraints and nodal price separation. The
approach bridges the gap between abstract academic test cases and operator-relevant grid
behavior, providing a scalable experimental scaffold that can be extended toward
PyPSA-USA–scale formulations while preserving interpretability, determinism, and
publication-quality artifacts.

### CES interpretation (BYOG participation)

To interpret DevNet stress results, we use a **Constant Elasticity of Substitution (CES)** framing.

The CES parameter **ρ** is **not part of the OPF model**.  
It is a **post-simulation interpretation tool** used to characterize how datacenter BYOG interacts with grid supply across scenarios.

With BYOG participating in front of the meter, the system transitions from:

```text
Single-supply (grid only)
→ Dual-supply interaction (grid + datacenter BYOG)
```

We interpret observed behavior using:

* **ρ ≈ 0**
  BYOG not viable for dispatch (grid dominates)

* **ρ ≈ 1**
  BYOG participates alongside grid supply
  → reduces system cost
  → may moderate congestion

* **ρ → ∞**
  BYOG rivals grid generation at the node
  → can materially influence dispatch
  → may collapse LMP spread under sufficient capacity

This CES mapping provides a **compact and consistent lens** for interpreting:

* system objective (cost)
* congestion behavior
* LMP spread outcomes

> Note: CES is used as a **descriptive abstraction**, not a modeled constraint.

---

## AI assistance

This work benefited from the use of ChatGPT (OpenAI) as a productivity aid for code
organization, documentation, and workflow clarity. All modeling decisions, analysis, and
interpretation remain the responsibility of the author.

---